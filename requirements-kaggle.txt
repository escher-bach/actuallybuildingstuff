# Pinned migration compatibility set; GPU integration is verified on Kaggle.
# Kaggle supplies the CUDA-enabled torch==2.6.0 runtime; do not replace it
# with the CPU wheel from PyPI.
accelerate==1.12.0
datasets==4.0.0
maturin==1.8.3
psutil==6.1.1
safetensors==0.6.2
tokenizers==0.22.1
transformers==4.57.6
trl==0.29.1
