# Ordered log of sources processed. Feeds the saturation curve (Repertoire Spec section 7).
#
# One entry per source actually read, in the order read. `rows` names the register
# rows that source produced -- an entry with no rows is not a failure to record,
# it is a source that yielded nothing, and those are exactly the entries that make
# the curve flatten honestly.

[[entry]]
n = 0
date = "2026-08-06"
source = "Task Specification section 6 (worked family, not excavated)"
vein = "0"
rows = ["mod-arith-hidden-permutation"]
note = "Calibration row. Not a source; establishes what a filled row looks like."

[[entry]]
n = 0
date = "2026-08-06"
source = "Repertoire Spec section 5 plant table (constructed, not excavated)"
vein = "0"
rows = ["junk-random", "junk-trivial"]
note = "Both implemented and tested. Contribute no primitives by construction, which validation now enforces."

[[entry]]
n = 1
date = "2026-08-06"
source = "Vein 2.2 -- query learning, teaching dimension, Bayesian optimal experimental design (18 sources: 9 verified, 11 lead)"
vein = "2.2"
rows = [
    "conjunction-identification",
    "parity-identification",
    "monotone-dnf-identification",
    "decision-tree-identification",
    "small-dfa-identification",
    "generic-finite-theta-versionspace",
]
note = """
6 proposed primitives, 3 actually new after folding -- the first real data point
on the saturation curve. Three rows held at `lead` because their primary sources
could not be rendered (Angluin 1987/1988 PDFs); the blocker is a library run, not
more reading.
"""

[[entry]]
n = 2
date = "2026-08-06"
source = "Vein 2.5a -- corrupt/localize across four substrates: mutation testing, model-based diagnosis, proof errors, plan faults (29 sources: 9 verified by the reviewer, of which 5 survived curator checking clean)"
vein = "2.5"
rows = [
    "mutant-localization",
    "circuit-fault-localization",
    "proof-step-localization",
    "plan-fault-localization",
]
note = """
ZERO new primitives across four unrelated substrates -- the strongest saturation
signal so far, and a fair test since nothing about mutation testing, circuit
diagnosis, proof checking and plan validation forced them to share vocabulary.

Curator verification pass found 1 fabricated statistic and 2 numeric drifts among
the sources promoted to rows; see register/verification-log.md. Rows corrected in
place with the correction left visible.
"""

[[entry]]
n = 3
date = "2026-08-06"
source = "Vein 2.3 -- hidden-rule paradigms from psychology and meta-RL: Bruner/Goodnow/Austin, Harlow, Shepard-Hovland-Jenkins, WCST/reversal, Daw two-step, bandits, probability matching"
vein = "2.3"
rows = [
    "shj-type-i",
    "shj-type-vi",
    "bruner-concept-attainment",
    "wcst-reversal",
    "probability-matching",
    "harlow-learning-set",
]
note = """
1 new primitive: belief-state-reset. Accepted because the whole vocabulary
assumed monotone belief refinement and piecewise-stationary theta requires
non-monotonic replacement -- a structural gap, not a missing synonym. This is the
vein that supplied all three plants (prerequisite pair, near-duplicate pair,
independent pair), which is what Repertoire Spec section 5 predicted it would do.

Most primaries here are paywalled, so most rows are held at `lead`. The blocker
is library access, same as for Angluin.

One CONTAMINATION logged against the seal: a non-primate animal cognition paper
was fetched during this pass. See docs/00-heldout-partition.md section 4.
"""

[[entry]]
n = 4
date = "2026-08-07"
source = "Vein 2.5b -- library learning, grammatical inference, planning generators, and synthetic-pretraining precedents (13 sources: 7 read as full text, 6 abstract-only, all distinguished by the reviewer)"
vein = "2.5"
rows = [
    "abstraction-extraction",
    "composition-chain",
    "subgoal-segmentation",
]
note = """
1 new primitive: abstraction-naming (anti-unification). Accepted because every
prior primitive was eliminative, interrogative, or a readout -- none CONSTRUCTED
a new reusable unit, and a vocabulary without a constructive operation cannot
express how a finite basis covers an infinite space. The gap was exposed by the
one vein the coverage grid pointed at, which is a small piece of evidence that
the grid is steering the review correctly.

Fills the last two empty cells: `composition` content and `compose/decompose`
asymmetry. All four coverage axes now have entries.

This review was markedly more careful than the earlier ones -- it distinguished
full-text reads from abstract-only, caught a fetch-tool misattribution in one of
its own sources, and self-reported a seal breach. The accuracy directive added
after the fabricated-statistic incident appears to have worked.

One CONTAMINATION, self-reported: a graph-colouring phase-transition paper was
opened before its sealed content was recognised, then excluded from all use with
an independent substitute source found. See docs/00-heldout-partition.md.
"""
